<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows plain HTTP only to the emulator's host-loopback alias (10.0.2.2) while
  keeping cleartext traffic blocked everywhere else. <domain> entries must be
  exact hosts/IPs — if testing on a physical device against a LAN IP, add that
  IP as an extra <domain> entry here. Once the API is deployed behind HTTPS,
  this file — and the http:// base URL in lib/core/config/api_config.dart —
  should be removed.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
